interface:
  display_name: "SNUPI Research Slides (EN)"
  short_description: "Build SNUPI-style research slide drafts from briefs, interviews, or papers"
  default_prompt: "Use $snupi-presentation-slides-en to gather missing essentials, normalize the request into a deck spec, extract paper assets when useful, build a SNUPI-style PPTX draft, and validate the layout before returning the deck."
policy:
  allow_implicit_invocation: true
